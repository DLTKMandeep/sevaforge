"""\nTests for Week 3 — Layer 4: Tool Registry and API Connector.\n\nCovers:\n  - ToolRegistry: register/unregister, list, search, match_capabilities, suggest_tools\n  - APIConnector: register/unregister connector, mock call, auth header building, retry logic\n  - API endpoints: /tools/register, GET /tools/, POST /tools/search, POST /tools/suggest\n  - API endpoints: /tools/connectors/register, GET /tools/connectors/, POST /tools/connectors/{id}/call\n"""\n\nimport pytest\nfrom httpx import ASGITransport, AsyncClient\n\nfrom sevaforge.api.app import create_app\nfrom sevaforge.tools import ToolRegistry, ToolDefinition, ToolCapability, APIConnector, ConnectorConfig\nfrom sevaforge.tools.api_connector import AuthScheme\n\n\n# ── Fixtures ─────────────────────────────────────────────────────────\n\n\n@pytest.fixture\ndef registry():\n    """Fresh ToolRegistry for each test."""\n    reg = ToolRegistry()\n    yield reg\n    reg.reset()\n\n\n@pytest.fixture\ndef connector():\n    """Fresh APIConnector for each test."""\n    conn = APIConnector()\n    yield conn\n    conn.reset()\n\n\n@pytest.fixture\ndef sample_tool():\n    """A reusable sample ToolDefinition."""\n    return ToolDefinition(\n        name="code-reviewer",\n        description="Automated code review tool that analyses pull requests for quality",\n        version="2.0.0",\n        capabilities=[\n            ToolCapability(name="review", description="Review code changes for bugs and style"),\n            ToolCapability(name="lint", description="Run static analysis and linting checks"),\n        ],\n        tags=["code", "quality", "review"],\n        category="development",\n        author="seva-team",\n    )\n\n\n@pytest.fixture\ndef app():\n    return create_app()\n\n\n@pytest.fixture\nasync def client(app):\n    transport = ASGITransport(app=app)\n    async with AsyncClient(transport=transport, base_url="http://test") as ac:\n        yield ac\n\n\n# ══════════════════════════════════════════════════════════════════════\n# Tool Registry Unit Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\ndef test_registry_register_tool(registry, sample_tool):\n    """Registering a tool stores it and computes its embedding."""\n    registered = registry.register_tool(sample_tool)\n    assert registered.tool_id == sample_tool.tool_id\n    assert len(registered.embedding) > 0\n    assert registry.stats()["tools_registered"] == 1\n\n\ndef test_registry_unregister_tool(registry, sample_tool):\n    """Unregistering removes the tool from the registry."""\n    registry.register_tool(sample_tool)\n    assert registry.unregister_tool(sample_tool.tool_id) is True\n    assert registry.get_tool(sample_tool.tool_id) is None\n    assert registry.stats()["tools_registered"] == 0\n\n\ndef test_registry_unregister_nonexistent(registry):\n    """Unregistering a non-existent tool returns False."""\n    assert registry.unregister_tool("does-not-exist") is False\n\n\ndef test_registry_list_tools(registry, sample_tool):\n    """Listing tools returns registered tools with optional filters."""\n    registry.register_tool(sample_tool)\n    all_tools = registry.list_tools()\n    assert len(all_tools) == 1\n    assert all_tools[0].name == "code-reviewer"\n\n    # Filter by category\n    dev_tools = registry.list_tools(category="development")\n    assert len(dev_tools) == 1\n    other_tools = registry.list_tools(category="infrastructure")\n    assert len(other_tools) == 0\n\n\ndef test_registry_list_tools_by_tag(registry, sample_tool):\n    """Listing tools can filter by tag."""\n    registry.register_tool(sample_tool)\n    tagged = registry.list_tools(tag="code")\n    assert len(tagged) == 1\n    missing = registry.list_tools(tag="nonexistent-tag")\n    assert len(missing) == 0\n\n\ndef test_registry_search_tools(registry):\n    """Semantic search finds tools matching a query."""\n    registry.register_tool(ToolDefinition(\n        name="code-review", description="Review code quality and find bugs",\n        capabilities=[ToolCapability(name="review", description="code review")],\n        category="development",\n    ))\n    registry.register_tool(ToolDefinition(\n        name="deploy-tool", description="Deploy applications to cloud infrastructure",\n        capabilities=[ToolCapability(name="deploy", description="deploy apps")],\n        category="operations",\n    ))\n\n    results = registry.search_tools("review my code for bugs", top_k=2)\n    assert len(results) >= 1\n    # The code-review tool should rank higher\n    top_tool, top_score = results[0]\n    assert top_score > 0.0\n    assert "review" in top_tool.name.lower() or "code" in top_tool.name.lower()\n\n\ndef test_registry_match_capabilities(registry, sample_tool):\n    """Capability matching finds tools with overlapping capabilities."""\n    registry.register_tool(sample_tool)\n    matches = registry.match_capabilities(["review", "lint"])\n    assert len(matches) >= 1\n    tool, matched_caps = matches[0]\n    assert "review" in matched_caps or "lint" in matched_caps\n\n\ndef test_registry_suggest_tools(registry):\n    """Tool suggestion combines semantic search and capability matching."""\n    registry.register_tool(ToolDefinition(\n        name="test-runner", description="Run unit and integration tests",\n        capabilities=[ToolCapability(name="test", description="execute tests")],\n        tags=["testing"], category="quality",\n    ))\n    suggestions = registry.suggest_tools("run automated tests on my project")\n    assert isinstance(suggestions, list)\n    # With only one tool registered, it should appear if any match\n    if suggestions:\n        assert "composite_score" in suggestions[0]\n\n\ndef test_registry_search_empty_registry(registry):\n    """Searching an empty registry returns an empty list."""\n    results = registry.search_tools("anything")\n    assert results == []\n\n\n# ══════════════════════════════════════════════════════════════════════\n# API Connector Unit Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\ndef test_connector_register(connector):\n    """Registering a connector stores it."""\n    config = ConnectorConfig(\n        name="github", base_url="https://api.github.com",\n        auth_scheme=AuthScheme.BEARER, auth_config={"token": "test-token"},\n    )\n    registered = connector.register_connector(config)\n    assert registered.connector_id == config.connector_id\n    assert connector.get_connector(config.connector_id) is not None\n\n\ndef test_connector_unregister(connector):\n    """Unregistering removes the connector."""\n    config = ConnectorConfig(name="temp-api", base_url="https://example.com")\n    connector.register_connector(config)\n    assert connector.unregister_connector(config.connector_id) is True\n    assert connector.get_connector(config.connector_id) is None\n\n\ndef test_connector_unregister_nonexistent(connector):\n    """Unregistering a non-existent connector returns False."""\n    assert connector.unregister_connector("no-such-id") is False\n\n\ndef test_connector_list(connector):\n    """Listing connectors returns all registered entries."""\n    connector.register_connector(ConnectorConfig(name="api-a", base_url="https://a.com"))\n    connector.register_connector(ConnectorConfig(name="api-b", base_url="https://b.com"))\n    listed = connector.list_connectors()\n    assert len(listed) == 2\n\n\ndef test_connector_mock_call(connector):\n    """In mock mode (no HTTP client), call returns a simulated response."""\n    config = ConnectorConfig(name="mock-api", base_url="https://mock.example.com")\n    connector.register_connector(config)\n\n    response = connector.call(config.connector_id, "GET", "/test")\n    assert response.status_code == 200\n    assert response.success is True\n    assert response.body.get("mock") is True\n\n\ndef test_connector_auth_bearer(connector):\n    """Bearer auth builds correct Authorization header."""\n    config = ConnectorConfig(\n        auth_scheme=AuthScheme.BEARER, auth_config={"token": "my-secret-token"},\n    )\n    headers = APIConnector._build_auth_headers(config)\n    assert headers.get("Authorization") == "Bearer my-secret-token"\n\n\ndef test_connector_auth_api_key(connector):\n    """API key auth injects the key via the configured header name."""\n    config = ConnectorConfig(\n        auth_scheme=AuthScheme.API_KEY,\n        auth_config={"api_key": "abc123", "header": "X-Custom-Key"},\n    )\n    headers = APIConnector._build_auth_headers(config)\n    assert headers.get("X-Custom-Key") == "abc123"\n\n\ndef test_connector_auth_basic(connector):\n    """Basic auth produces correct Base64-encoded credentials."""\n    import base64\n    config = ConnectorConfig(\n        auth_scheme=AuthScheme.BASIC,\n        auth_config={"username": "admin", "password": "secret"},\n    )\n    headers = APIConnector._build_auth_headers(config)\n    expected = base64.b64encode(b"admin:secret").decode("ascii")\n    assert headers.get("Authorization") == f"Basic {expected}"\n\n\ndef test_connector_call_unknown_connector(connector):\n    """Calling an unregistered connector raises ValueError."""\n    with pytest.raises(ValueError, match="Unknown connector"):\n        connector.call("nonexistent-id", "GET")\n\n\n# ══════════════════════════════════════════════════════════════════════\n# Tools API Endpoint Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\n@pytest.mark.asyncio\nasync def test_api_register_tool(client):\n    """POST /tools/register creates a tool and returns its definition."""\n    response = await client.post("/api/v1/tools/register", json={\n        "name": "scanner",\n        "description": "Security vulnerability scanner",\n        "capabilities": [{"name": "scan", "description": "Scan for vulnerabilities"}],\n        "tags": ["security"],\n        "category": "security",\n    })\n    assert response.status_code == 200\n    data = response.json()\n    assert data["name"] == "scanner"\n    assert "tool_id" in data\n\n\n@pytest.mark.asyncio\nasync def test_api_list_tools(client):\n    """GET /tools/ lists registered tools."""\n    await client.post("/api/v1/tools/register", json={\n        "name": "lister-tool", "description": "A tool for listing"\n    })\n    response = await client.get("/api/v1/tools/")\n    assert response.status_code == 200\n    assert isinstance(response.json(), list)\n\n\n@pytest.mark.asyncio\nasync def test_api_search_tools(client):\n    """POST /tools/search performs semantic search."""\n    await client.post("/api/v1/tools/register", json={\n        "name": "code-analyser", "description": "Analyse code quality and complexity",\n        "capabilities": [{"name": "analyse", "description": "code analysis"}],\n    })\n    response = await client.post("/api/v1/tools/search", json={\n        "query": "analyse my code quality",\n    })\n    assert response.status_code == 200\n    assert isinstance(response.json(), list)\n\n\n@pytest.mark.asyncio\nasync def test_api_suggest_tools(client):\n    """POST /tools/suggest suggests tools for a task description."""\n    await client.post("/api/v1/tools/register", json={\n        "name": "test-executor", "description": "Execute automated test suites",\n        "capabilities": [{"name": "test", "description": "run tests"}],\n    })\n    response = await client.post("/api/v1/tools/suggest", json={\n        "task_description": "I need to run tests on my project",\n    })\n    assert response.status_code == 200\n    assert isinstance(response.json(), list)\n\n\n@pytest.mark.asyncio\nasync def test_api_register_connector(client):\n    """POST /tools/connectors/register registers an API connector."""\n    response = await client.post("/api/v1/tools/connectors/register", json={\n        "name": "test-api",\n        "base_url": "https://api.test.com",\n        "auth_scheme": "bearer",\n        "auth_config": {"token": "test"},\n    })\n    assert response.status_code == 200\n    data = response.json()\n    assert data["name"] == "test-api"\n    assert "connector_id" in data\n\n\n@pytest.mark.asyncio\nasync def test_api_list_connectors(client):\n    """GET /tools/connectors/ lists all connectors."""\n    response = await client.get("/api/v1/tools/connectors/")\n    assert response.status_code == 200\n    assert isinstance(response.json(), list)\n\n\n@pytest.mark.asyncio\nasync def test_api_call_connector(client):\n    """POST /tools/connectors/{id}/call calls a connector in mock mode."""\n    reg_resp = await client.post("/api/v1/tools/connectors/register", json={\n        "name": "mock-svc", "base_url": "https://mock.svc.io",\n    })\n    connector_id = reg_resp.json()["connector_id"]\n\n    call_resp = await client.post(f"/api/v1/tools/connectors/{connector_id}/call", json={\n        "method": "GET", "path": "/status",\n    })\n    assert call_resp.status_code == 200\n    data = call_resp.json()\n    assert data["success"] is True\n