"""\nTests for Week 3 — Layer 2: JWT Auth, Rate Limiting, and Circuit Breaker.\n\nCovers:\n  - JWTHandler: create_token, validate_token, expired token, refresh, revoke, invalid token\n  - RateLimiter: check, consume, bucket refill, stale cleanup\n  - CircuitBreaker: closed state, failure counting, open state, half-open recovery\n  - API endpoints: /auth/token, /auth/verify, /auth/refresh, /auth/revoke\n  - API endpoints: /auth/rate-limit/{key}, /auth/rate-limit/{key}/consume\n"""\n\nimport time\n\nimport pytest\nfrom httpx import ASGITransport, AsyncClient\n\nfrom sevaforge.api.app import create_app\nfrom sevaforge.auth import JWTHandler, RateLimiter, CircuitBreaker\nfrom sevaforge.auth.jwt_handler import TokenPayload, TokenExpiredError, TokenRevokedError, TokenInvalidError\nfrom sevaforge.auth.rate_limiter import CircuitBreakerState, CircuitBreakerError\n\n\n# ── Fixtures ─────────────────────────────────────────────────────────\n\n\n@pytest.fixture\ndef jwt_handler():\n    """Fresh JWTHandler with a known secret for deterministic tests."""\n    handler = JWTHandler(secret="test-secret-key-for-unit-tests", expire_minutes=30)\n    yield handler\n    handler.reset()\n\n\n@pytest.fixture\ndef rate_limiter():\n    """Fresh RateLimiter with small bucket for fast testing."""\n    limiter = RateLimiter(max_tokens=5, refill_rate=1.0, stale_bucket_seconds=0.5)\n    yield limiter\n    limiter.reset()\n\n\n@pytest.fixture\ndef circuit_breaker():\n    """Fresh CircuitBreaker with low thresholds for fast testing."""\n    cb = CircuitBreaker(name="test-cb", failure_threshold=3, recovery_timeout=0.3, half_open_max_calls=2)\n    yield cb\n    cb.reset()\n\n\n@pytest.fixture\ndef app():\n    return create_app()\n\n\n@pytest.fixture\nasync def client(app):\n    transport = ASGITransport(app=app)\n    async with AsyncClient(transport=transport, base_url="http://test") as ac:\n        yield ac\n\n\n# ══════════════════════════════════════════════════════════════════════\n# JWT Handler Unit Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\ndef test_jwt_create_token(jwt_handler):\n    """Token creation returns a three-part JWT string."""\n    token = jwt_handler.create_token("user-1", "tenant-1", ["admin"])\n    parts = token.split(".")\n    assert len(parts) == 3\n    assert jwt_handler.stats()["tokens_created"] == 1\n\n\ndef test_jwt_validate_token(jwt_handler):\n    """A freshly created token validates and returns correct payload."""\n    token = jwt_handler.create_token("user-42", "tenant-acme", ["viewer", "editor"])\n    payload = jwt_handler.validate_token(token)\n\n    assert isinstance(payload, TokenPayload)\n    assert payload.user_id == "user-42"\n    assert payload.tenant_id == "tenant-acme"\n    assert payload.roles == ["viewer", "editor"]\n    assert payload.remaining_seconds > 0\n    assert not payload.is_expired\n\n\ndef test_jwt_expired_token(jwt_handler):\n    """A token whose expiry is in the past should raise TokenExpiredError."""\n    # Create a normal token, then manually build an expired one by\n    # encoding a payload with exp set in the past.\n    token = jwt_handler.create_token("user-1", "tenant-1")\n    payload = jwt_handler.validate_token(token)\n\n    # Forge a token with exp 10 seconds in the past using the handler's encoder\n    import json\n    expired_payload = {\n        "user_id": "user-1",\n        "tenant_id": "tenant-1",\n        "roles": [],\n        "exp": time.time() - 10,\n        "iat": payload.iat,\n        "jti": payload.jti,\n    }\n    expired_token = jwt_handler._encode(expired_payload)\n\n    with pytest.raises(TokenExpiredError):\n        jwt_handler.validate_token(expired_token)\n\n\ndef test_jwt_refresh_token(jwt_handler):\n    """Refreshing a valid token returns a different token with a new JTI."""\n    original = jwt_handler.create_token("user-1", "tenant-1", ["admin"])\n    original_payload = jwt_handler.validate_token(original)\n\n    refreshed = jwt_handler.refresh_token(original)\n    refreshed_payload = jwt_handler.validate_token(refreshed)\n\n    assert refreshed != original\n    assert refreshed_payload.user_id == original_payload.user_id\n    assert refreshed_payload.tenant_id == original_payload.tenant_id\n    assert refreshed_payload.jti != original_payload.jti\n\n\ndef test_jwt_revoke_token(jwt_handler):\n    """A revoked token raises TokenRevokedError on validation."""\n    token = jwt_handler.create_token("user-1", "tenant-1")\n    payload = jwt_handler.validate_token(token)\n\n    jwt_handler.revoke_token(payload.jti)\n    assert jwt_handler.is_revoked(payload.jti)\n\n    with pytest.raises(TokenRevokedError):\n        jwt_handler.validate_token(token)\n\n\ndef test_jwt_invalid_token_bad_signature(jwt_handler):\n    """A token with a tampered signature raises TokenInvalidError."""\n    token = jwt_handler.create_token("user-1", "tenant-1")\n    # Tamper with the signature by replacing it entirely\n    parts = token.split(".")\n    tampered_sig = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"\n    tampered_token = f"{parts[0]}.{parts[1]}.{tampered_sig}"\n\n    with pytest.raises(TokenInvalidError):\n        jwt_handler.validate_token(tampered_token)\n\n\ndef test_jwt_invalid_token_malformed(jwt_handler):\n    """A completely malformed token raises TokenInvalidError."""\n    with pytest.raises(TokenInvalidError):\n        jwt_handler.validate_token("not.a.valid.jwt.at.all")\n\n    with pytest.raises(TokenInvalidError):\n        jwt_handler.validate_token("just-one-part")\n\n\ndef test_jwt_extra_claims(jwt_handler):\n    """Extra claims are preserved in the token payload."""\n    token = jwt_handler.create_token(\n        "user-1", "tenant-1", extra_claims={"department": "engineering"}\n    )\n    payload = jwt_handler.validate_token(token)\n    assert payload.extra.get("department") == "engineering"\n\n\ndef test_jwt_stats_tracking(jwt_handler):\n    """Stats counters increment correctly across operations."""\n    token = jwt_handler.create_token("u1", "t1")\n    jwt_handler.validate_token(token)\n    jwt_handler.refresh_token(token)\n\n    stats = jwt_handler.stats()\n    assert stats["tokens_created"] >= 2  # create + refresh creates a new one\n    assert stats["tokens_validated"] >= 1\n    assert stats["tokens_refreshed"] == 1\n\n\n# ══════════════════════════════════════════════════════════════════════\n# Rate Limiter Unit Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\ndef test_rate_limiter_check_allows_fresh_key(rate_limiter):\n    """A fresh key with full bucket is allowed."""\n    assert rate_limiter.check("fresh-key") is True\n\n\ndef test_rate_limiter_consume_returns_result(rate_limiter):\n    """Consuming a token returns a structured result."""\n    result = rate_limiter.consume("key-1")\n    assert result.allowed is True\n    assert result.remaining >= 0\n    assert result.retry_after == 0.0\n\n\ndef test_rate_limiter_exhaustion(rate_limiter):\n    """Exhausting all tokens causes rejection."""\n    for _ in range(5):\n        result = rate_limiter.consume("burst-key")\n        assert result.allowed is True\n\n    # Next consume should be rejected (bucket has 5 tokens)\n    result = rate_limiter.consume("burst-key")\n    assert result.allowed is False\n    assert result.retry_after > 0\n\n\ndef test_rate_limiter_bucket_refill(rate_limiter):\n    """Tokens refill over time at the configured rate."""\n    # Drain the bucket\n    for _ in range(5):\n        rate_limiter.consume("refill-key")\n\n    # Wait for refill (1 token/sec, wait ~1.1s to be safe)\n    time.sleep(1.1)\n\n    result = rate_limiter.consume("refill-key")\n    assert result.allowed is True\n\n\ndef test_rate_limiter_stale_cleanup(rate_limiter):\n    """Stale buckets are cleaned up after the configured timeout."""\n    rate_limiter.consume("stale-key")\n    assert rate_limiter.stats()["active_buckets"] >= 1\n\n    # Wait for stale timeout (0.5s)\n    time.sleep(0.6)\n    removed = rate_limiter.cleanup_stale()\n    assert removed >= 1\n\n\ndef test_rate_limiter_remaining_for_unknown_key(rate_limiter):\n    """Remaining tokens for an unknown key returns max_tokens."""\n    assert rate_limiter.remaining("nonexistent") == 5\n\n\n# ══════════════════════════════════════════════════════════════════════\n# Circuit Breaker Unit Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\ndef test_circuit_breaker_starts_closed(circuit_breaker):\n    """Circuit breaker starts in CLOSED state."""\n    assert circuit_breaker.state == CircuitBreakerState.CLOSED\n\n\ndef test_circuit_breaker_success_keeps_closed(circuit_breaker):\n    """Successful calls keep the circuit closed."""\n    result = circuit_breaker.call(lambda: "ok")\n    assert result == "ok"\n    assert circuit_breaker.state == CircuitBreakerState.CLOSED\n\n\ndef test_circuit_breaker_failure_counting(circuit_breaker):\n    """Failures accumulate and eventually trip the circuit to OPEN."""\n    for i in range(3):\n        try:\n            circuit_breaker.call(lambda: (_ for _ in ()).throw(ValueError("fail")))\n        except ValueError:\n            pass\n\n    assert circuit_breaker.state == CircuitBreakerState.OPEN\n\n\ndef test_circuit_breaker_open_rejects_calls(circuit_breaker):\n    """When OPEN, the circuit breaker rejects calls immediately."""\n    # Trip the breaker\n    for _ in range(3):\n        try:\n            circuit_breaker.call(lambda: (_ for _ in ()).throw(RuntimeError("boom")))\n        except RuntimeError:\n            pass\n\n    with pytest.raises(CircuitBreakerError):\n        circuit_breaker.call(lambda: "should not run")\n\n\ndef test_circuit_breaker_half_open_recovery(circuit_breaker):\n    """After recovery timeout, circuit moves to HALF_OPEN, then CLOSED on success."""\n    # Trip the breaker\n    for _ in range(3):\n        try:\n            circuit_breaker.call(lambda: (_ for _ in ()).throw(RuntimeError("fail")))\n        except RuntimeError:\n            pass\n\n    assert circuit_breaker.state == CircuitBreakerState.OPEN\n\n    # Wait for recovery timeout (0.3s)\n    time.sleep(0.35)\n    assert circuit_breaker.state == CircuitBreakerState.HALF_OPEN\n\n    # Successful calls in HALF_OPEN close the circuit\n    circuit_breaker.call(lambda: "recovering-1")\n    circuit_breaker.call(lambda: "recovering-2")\n    assert circuit_breaker.state == CircuitBreakerState.CLOSED\n\n\ndef test_circuit_breaker_half_open_failure_reopens(circuit_breaker):\n    """A failure in HALF_OPEN state reopens the circuit."""\n    # Trip the breaker\n    for _ in range(3):\n        try:\n            circuit_breaker.call(lambda: (_ for _ in ()).throw(RuntimeError("fail")))\n        except RuntimeError:\n            pass\n\n    time.sleep(0.35)\n    assert circuit_breaker.state == CircuitBreakerState.HALF_OPEN\n\n    # Fail during half-open\n    try:\n        circuit_breaker.call(lambda: (_ for _ in ()).throw(RuntimeError("half-open-fail")))\n    except RuntimeError:\n        pass\n\n    assert circuit_breaker.state == CircuitBreakerState.OPEN\n\n\n# ══════════════════════════════════════════════════════════════════════\n# Auth API Endpoint Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\n@pytest.mark.asyncio\nasync def test_api_create_token(client):\n    """POST /auth/token creates a token and returns payload metadata."""\n    response = await client.post("/api/v1/auth/token", json={\n        "user_id": "api-user-1",\n        "tenant_id": "api-tenant-1",\n        "roles": ["admin"],\n    })\n    assert response.status_code == 200\n    data = response.json()\n    assert "token" in data\n    assert data["user_id"] == "api-user-1"\n    assert data["tenant_id"] == "api-tenant-1"\n    assert "jti" in data\n\n\n@pytest.mark.asyncio\nasync def test_api_create_token_missing_fields(client):\n    """POST /auth/token returns 400 when required fields are missing."""\n    response = await client.post("/api/v1/auth/token", json={"user_id": "u1"})\n    assert response.status_code == 400\n\n\n@pytest.mark.asyncio\nasync def test_api_verify_token(client):\n    """POST /auth/verify validates a token and returns decoded info."""\n    create_resp = await client.post("/api/v1/auth/token", json={\n        "user_id": "v-user", "tenant_id": "v-tenant", "roles": ["viewer"],\n    })\n    token = create_resp.json()["token"]\n\n    verify_resp = await client.post("/api/v1/auth/verify", json={"token": token})\n    assert verify_resp.status_code == 200\n    assert verify_resp.json()["valid"] is True\n    assert verify_resp.json()["user_id"] == "v-user"\n\n\n@pytest.mark.asyncio\nasync def test_api_refresh_token(client):\n    """POST /auth/refresh issues a new token from a valid one."""\n    create_resp = await client.post("/api/v1/auth/token", json={\n        "user_id": "r-user", "tenant_id": "r-tenant",\n    })\n    token = create_resp.json()["token"]\n\n    refresh_resp = await client.post("/api/v1/auth/refresh", json={"token": token})\n    assert refresh_resp.status_code == 200\n    new_token = refresh_resp.json()["token"]\n    assert new_token != token\n\n\n@pytest.mark.asyncio\nasync def test_api_revoke_token(client):\n    """POST /auth/revoke revokes a token so it can no longer be verified."""\n    create_resp = await client.post("/api/v1/auth/token", json={\n        "user_id": "rev-user", "tenant_id": "rev-tenant",\n    })\n    token = create_resp.json()["token"]\n\n    revoke_resp = await client.post("/api/v1/auth/revoke", json={"token": token})\n    assert revoke_resp.status_code == 200\n    assert revoke_resp.json()["revoked"] is True\n\n    # Verify should now fail\n    verify_resp = await client.post("/api/v1/auth/verify", json={"token": token})\n    assert verify_resp.status_code == 401\n\n\n@pytest.mark.asyncio\nasync def test_api_rate_limit_check(client):\n    """GET /auth/rate-limit/{key} returns rate limit status."""\n    response = await client.get("/api/v1/auth/rate-limit/test-api-key")\n    assert response.status_code == 200\n    data = response.json()\n    assert data["key"] == "test-api-key"\n    assert "allowed" in data\n    assert "remaining_tokens" in data\n\n\n@pytest.mark.asyncio\nasync def test_api_rate_limit_consume(client):\n    """POST /auth/rate-limit/{key}/consume consumes tokens."""\n    response = await client.post(\n        "/api/v1/auth/rate-limit/consume-key/consume",\n        json={"tokens": 1},\n    )\n    assert response.status_code == 200\n    data = response.json()\n    assert data["key"] == "consume-key"\n    assert "allowed" in data\n    assert "remaining" in data\n