# Environment-specific deployment patch
# Generated by ForgeFlow
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forgeflow
spec:
  template:
    spec:
      containers:
        - name: forgeflow
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
