apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sevaforge-unified-monitor
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: sevaforge-unified
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
