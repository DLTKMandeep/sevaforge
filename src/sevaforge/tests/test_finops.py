"""\nTests for Week 3 — Layer 9: FinOps & Metering.\n\nCovers:\n  - CostTracker: record_usage, calculate_cost, get_summary, top_consumers,\n    model_breakdown, estimate_monthly\n  - BudgetManager: create/update/delete quota, check_budget (allowed, warning,\n    critical, exceeded), record_spend, auto-throttle, alerts, budget_report, period reset\n  - API endpoints: /finops/usage/record, GET /finops/usage/summary\n  - API endpoints: /finops/budget/quotas, /finops/budget/check, /finops/budget/spend\n  - API endpoints: GET /finops/budget/alerts, GET /finops/budget/report/{tenant_id}\n"""\n\nimport pytest\nfrom httpx import ASGITransport, AsyncClient\n\nfrom sevaforge.api.app import create_app\nfrom sevaforge.finops import (\n    CostTracker,\n    UsageRecord,\n    CostSummary,\n    BudgetManager,\n    BudgetQuota,\n    BudgetAlert,\n    BudgetStatus,\n)\n\n\n# ── Fixtures ─────────────────────────────────────────────────────────\n\n\n@pytest.fixture\ndef tracker():\n    """Fresh CostTracker for each test."""\n    ct = CostTracker()\n    yield ct\n    ct.reset()\n\n\n@pytest.fixture\ndef budget():\n    """Fresh BudgetManager for each test."""\n    bm = BudgetManager()\n    yield bm\n    bm.reset()\n\n\n@pytest.fixture\ndef app():\n    return create_app()\n\n\n@pytest.fixture\nasync def client(app):\n    transport = ASGITransport(app=app)\n    async with AsyncClient(transport=transport, base_url="http://test") as ac:\n        yield ac\n\n\n# ══════════════════════════════════════════════════════════════════════\n# CostTracker Unit Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\ndef test_tracker_record_usage(tracker):\n    """Recording usage creates a UsageRecord with computed cost."""\n    record = tracker.record_usage(\n        agent_id="code-review",\n        user_id="u-1",\n        tenant_id="t-acme",\n        model="claude-sonnet-4-20250514",\n        input_tokens=1000,\n        output_tokens=500,\n        latency_ms=250.0,\n    )\n    assert isinstance(record, UsageRecord)\n    assert record.agent_id == "code-review"\n    assert record.cost_usd > 0\n    assert record.model == "claude-sonnet-4-20250514"\n\n\ndef test_tracker_calculate_cost(tracker):\n    """Cost calculation uses the pricing table correctly."""\n    # claude-sonnet-4-20250514: input=$3/1M, output=$15/1M\n    cost = tracker.calculate_cost("claude-sonnet-4-20250514", 1_000_000, 1_000_000)\n    assert cost == pytest.approx(3.0 + 15.0)\n\n\ndef test_tracker_calculate_cost_fallback(tracker):\n    """Unknown models use the fallback pricing rate."""\n    cost = tracker.calculate_cost("unknown-model-xyz", 1_000_000, 1_000_000)\n    # Fallback: input=$3/1M, output=$15/1M\n    assert cost == pytest.approx(3.0 + 15.0)\n\n\ndef test_tracker_cached_usage_zero_cost(tracker):\n    """Cached responses are recorded with zero cost."""\n    record = tracker.record_usage(\n        agent_id="agent-1", user_id="u-1", tenant_id="t-1",\n        model="gpt-4o", input_tokens=500, output_tokens=200, cached=True,\n    )\n    assert record.cost_usd == 0.0\n    assert record.cached is True\n\n\ndef test_tracker_get_summary(tracker):\n    """get_summary aggregates costs across records."""\n    tracker.record_usage(\n        agent_id="a1", user_id="u1", tenant_id="t1",\n        model="gpt-4o-mini", input_tokens=1000, output_tokens=500,\n    )\n    tracker.record_usage(\n        agent_id="a2", user_id="u1", tenant_id="t1",\n        model="gpt-4o-mini", input_tokens=2000, output_tokens=1000,\n    )\n\n    summary = tracker.get_summary(tenant_id="t1")\n    assert isinstance(summary, CostSummary)\n    assert summary.total_requests == 2\n    assert summary.total_cost_usd > 0\n    assert summary.total_input_tokens == 3000\n    assert summary.total_output_tokens == 1500\n\n\ndef test_tracker_top_consumers(tracker):\n    """top_consumers returns tenants ranked by spend."""\n    tracker.record_usage(\n        agent_id="a1", user_id="u1", tenant_id="t-big",\n        model="claude-opus-4-20250514", input_tokens=10000, output_tokens=5000,\n    )\n    tracker.record_usage(\n        agent_id="a1", user_id="u1", tenant_id="t-small",\n        model="gpt-4o-mini", input_tokens=100, output_tokens=50,\n    )\n\n    top = tracker.get_top_consumers(by="tenant", limit=5)\n    assert len(top) == 2\n    # t-big should be first (higher cost model)\n    assert top[0][0] == "t-big"\n    assert top[0][1] > top[1][1]\n\n\ndef test_tracker_model_breakdown(tracker):\n    """model_breakdown returns per-model cost and token totals."""\n    tracker.record_usage(\n        agent_id="a1", user_id="u1", tenant_id="t1",\n        model="gpt-4o", input_tokens=1000, output_tokens=500,\n    )\n    tracker.record_usage(\n        agent_id="a1", user_id="u1", tenant_id="t1",\n        model="gpt-4o", input_tokens=2000, output_tokens=1000,\n    )\n\n    breakdown = tracker.get_model_breakdown(tenant_id="t1")\n    assert "gpt-4o" in breakdown\n    assert breakdown["gpt-4o"]["requests"] == 2\n    assert breakdown["gpt-4o"]["input_tokens"] == 3000\n\n\ndef test_tracker_estimate_monthly(tracker):\n    """Monthly cost estimation projects from current spend velocity."""\n    tracker.record_usage(\n        agent_id="a1", user_id="u1", tenant_id="t-est",\n        model="gpt-4o-mini", input_tokens=10000, output_tokens=5000,\n    )\n    estimated = tracker.estimate_monthly_cost("t-est")\n    # Should be a positive projection\n    assert estimated > 0\n\n\ndef test_tracker_update_pricing(tracker):\n    """Pricing can be updated for a model."""\n    tracker.update_pricing("custom-model", input_price=1.0, output_price=2.0)\n    pricing = tracker.get_pricing()\n    assert "custom-model" in pricing\n    assert pricing["custom-model"]["input"] == 1.0\n    assert pricing["custom-model"]["output"] == 2.0\n\n\ndef test_tracker_stats(tracker):\n    """Stats track total cost and record count."""\n    tracker.record_usage(\n        agent_id="a1", user_id="u1", tenant_id="t1",\n        model="gpt-4o-mini", input_tokens=1000, output_tokens=500,\n    )\n    stats = tracker.stats()\n    assert stats["total_tracked"] == 1\n    assert stats["total_cost"] > 0\n    assert stats["records_count"] == 1\n\n\n# ══════════════════════════════════════════════════════════════════════\n# BudgetManager Unit Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\ndef test_budget_create_quota(budget):\n    """Creating a budget quota stores it with correct parameters."""\n    quota = budget.create_quota(\n        tenant_id="t-acme",\n        budget_limit_usd=500.0,\n        period="monthly",\n        warning_threshold=0.80,\n        critical_threshold=0.95,\n    )\n    assert isinstance(quota, BudgetQuota)\n    assert quota.tenant_id == "t-acme"\n    assert quota.budget_limit_usd == 500.0\n    assert quota.status == BudgetStatus.ACTIVE\n\n\ndef test_budget_update_quota(budget):\n    """Updating a quota changes only the specified fields."""\n    quota = budget.create_quota("t-1", budget_limit_usd=100.0)\n    updated = budget.update_quota(quota.quota_id, budget_limit_usd=200.0)\n    assert updated.budget_limit_usd == 200.0\n    # Period should remain unchanged\n    assert updated.period == "monthly"\n\n\ndef test_budget_delete_quota(budget):\n    """Deleting a quota removes it from the manager."""\n    quota = budget.create_quota("t-1", budget_limit_usd=100.0)\n    assert budget.delete_quota(quota.quota_id) is True\n    assert budget.get_quota(quota.quota_id) is None\n    assert budget.delete_quota(quota.quota_id) is False  # Already deleted\n\n\ndef test_budget_check_allowed(budget):\n    """A budget check with plenty of budget returns allowed=True."""\n    budget.create_quota("t-free", budget_limit_usd=1000.0)\n    result = budget.check_budget("t-free", estimated_cost=1.0)\n    assert result.allowed is True\n    assert result.status == BudgetStatus.ACTIVE\n    assert result.throttle_factor == 1.0\n\n\ndef test_budget_check_warning(budget):\n    """Spending past the warning threshold triggers WARNING status."""\n    quota = budget.create_quota(\n        "t-warn", budget_limit_usd=100.0,\n        warning_threshold=0.80, critical_threshold=0.95,\n    )\n    # Spend 85% of budget\n    budget.record_spend("t-warn", 85.0)\n\n    result = budget.check_budget("t-warn", estimated_cost=0.0)\n    assert result.status == BudgetStatus.WARNING\n\n\ndef test_budget_check_critical(budget):\n    """Spending past the critical threshold triggers CRITICAL status."""\n    budget.create_quota(\n        "t-crit", budget_limit_usd=100.0,\n        warning_threshold=0.80, critical_threshold=0.95,\n    )\n    budget.record_spend("t-crit", 96.0)\n\n    result = budget.check_budget("t-crit", estimated_cost=0.0)\n    assert result.status == BudgetStatus.CRITICAL\n\n\ndef test_budget_check_exceeded_hard_limit(budget):\n    """Exceeding a hard-limit quota blocks the request."""\n    budget.create_quota(\n        "t-hard", budget_limit_usd=100.0,\n        hard_limit=True,\n    )\n    budget.record_spend("t-hard", 101.0)\n\n    result = budget.check_budget("t-hard", estimated_cost=1.0)\n    assert result.allowed is False\n    assert result.status == BudgetStatus.EXCEEDED\n\n\ndef test_budget_record_spend_alerts(budget):\n    """record_spend triggers alerts when thresholds are crossed."""\n    budget.create_quota(\n        "t-alert", budget_limit_usd=100.0,\n        warning_threshold=0.50, critical_threshold=0.90,\n    )\n    # First spend — crosses 50% warning threshold\n    alerts = budget.record_spend("t-alert", 55.0)\n    assert len(alerts) >= 1\n    assert any(a.alert_type == "warning" for a in alerts)\n\n\ndef test_budget_auto_throttle(budget):\n    """Auto-throttle reduces the throttle factor when nearing the limit."""\n    budget.create_quota(\n        "t-throttle", budget_limit_usd=100.0,\n        warning_threshold=0.80, auto_throttle=True,\n    )\n    budget.record_spend("t-throttle", 90.0)\n\n    result = budget.check_budget("t-throttle", estimated_cost=0.0)\n    assert result.throttle_factor < 1.0\n\n\ndef test_budget_get_alerts(budget):\n    """get_alerts returns alerts for a tenant."""\n    budget.create_quota(\n        "t-alerts", budget_limit_usd=100.0,\n        warning_threshold=0.50,\n    )\n    budget.record_spend("t-alerts", 55.0)\n\n    alerts = budget.get_alerts(tenant_id="t-alerts")\n    assert len(alerts) >= 1\n    assert isinstance(alerts[0], BudgetAlert)\n\n\ndef test_budget_report(budget):\n    """budget_report returns a comprehensive report for a tenant."""\n    budget.create_quota("t-report", budget_limit_usd=500.0)\n    budget.record_spend("t-report", 50.0)\n\n    report = budget.get_budget_report("t-report")\n    assert report["tenant_id"] == "t-report"\n    assert report["total_budget"] == 500.0\n    assert report["total_spend"] == pytest.approx(50.0, abs=0.01)\n    assert len(report["quotas"]) == 1\n\n\ndef test_budget_period_reset(budget):\n    """Resetting a period zeroes the spend and resets status."""\n    quota = budget.create_quota("t-reset", budget_limit_usd=100.0)\n    budget.record_spend("t-reset", 101.0)\n    assert quota.status == BudgetStatus.EXCEEDED\n\n    budget.reset_period(quota.quota_id)\n    assert quota.current_spend == 0.0\n    assert quota.status == BudgetStatus.ACTIVE\n\n\ndef test_budget_no_quota_allows_all(budget):\n    """Tenants without quotas are allowed without restriction."""\n    result = budget.check_budget("t-no-quota", estimated_cost=999.0)\n    assert result.allowed is True\n    assert result.throttle_factor == 1.0\n\n\n# ══════════════════════════════════════════════════════════════════════\n# FinOps API Endpoint Tests\n# ══════════════════════════════════════════════════════════════════════\n\n\n@pytest.mark.asyncio\nasync def test_api_record_usage(client):\n    """POST /finops/usage/record records an LLM usage event."""\n    response = await client.post("/api/v1/finops/usage/record", json={\n        "agent_id": "test-agent",\n        "user_id": "test-user",\n        "tenant_id": "test-tenant",\n        "model": "gpt-4o-mini",\n        "input_tokens": 1000,\n        "output_tokens": 500,\n    })\n    assert response.status_code == 200\n    data = response.json()\n    assert data["model"] == "gpt-4o-mini"\n    assert data["input_tokens"] == 1000\n    assert data["cost_usd"] >= 0\n\n\n@pytest.mark.asyncio\nasync def test_api_record_usage_missing_model(client):\n    """POST /finops/usage/record returns 400 when model is missing."""\n    response = await client.post("/api/v1/finops/usage/record", json={\n        "agent_id": "a1", "input_tokens": 100,\n    })\n    assert response.status_code == 400\n\n\n@pytest.mark.asyncio\nasync def test_api_usage_summary(client):\n    """GET /finops/usage/summary returns an aggregated summary."""\n    await client.post("/api/v1/finops/usage/record", json={\n        "agent_id": "a1", "tenant_id": "t1", "model": "gpt-4o-mini",\n        "input_tokens": 1000, "output_tokens": 500,\n    })\n    response = await client.get("/api/v1/finops/usage/summary", params={"tenant_id": "t1"})\n    assert response.status_code == 200\n    data = response.json()\n    assert "total_cost_usd" in data\n    assert "total_requests" in data\n\n\n@pytest.mark.asyncio\nasync def test_api_create_budget_quota(client):\n    """POST /finops/budget/quotas creates a budget quota."""\n    response = await client.post("/api/v1/finops/budget/quotas", json={\n        "tenant_id": "t-api",\n        "budget_limit_usd": 200.0,\n        "period": "monthly",\n    })\n    assert response.status_code == 200\n    data = response.json()\n    assert data["tenant_id"] == "t-api"\n    assert data["budget_limit_usd"] == 200.0\n\n\n@pytest.mark.asyncio\nasync def test_api_check_budget(client):\n    """POST /finops/budget/check evaluates budget before a request."""\n    await client.post("/api/v1/finops/budget/quotas", json={\n        "tenant_id": "t-check", "budget_limit_usd": 100.0,\n    })\n    response = await client.post("/api/v1/finops/budget/check", json={\n        "tenant_id": "t-check", "estimated_cost": 0.05,\n    })\n    assert response.status_code == 200\n    data = response.json()\n    assert data["allowed"] is True\n\n\n@pytest.mark.asyncio\nasync def test_api_record_spend(client):\n    """POST /finops/budget/spend records actual spend against quotas."""\n    await client.post("/api/v1/finops/budget/quotas", json={\n        "tenant_id": "t-spend", "budget_limit_usd": 100.0,\n    })\n    response = await client.post("/api/v1/finops/budget/spend", json={\n        "tenant_id": "t-spend", "amount_usd": 10.0,\n    })\n    assert response.status_code == 200\n    data = response.json()\n    assert data["recorded"] is True\n    assert data["amount_usd"] == 10.0\n\n\n@pytest.mark.asyncio\nasync def test_api_budget_alerts(client):\n    """GET /finops/budget/alerts returns budget alerts."""\n    response = await client.get("/api/v1/finops/budget/alerts")\n    assert response.status_code == 200\n    assert isinstance(response.json(), list)\n\n\n@pytest.mark.asyncio\nasync def test_api_budget_report(client):\n    """GET /finops/budget/report/{tenant_id} returns a budget report."""\n    await client.post("/api/v1/finops/budget/quotas", json={\n        "tenant_id": "t-rpt", "budget_limit_usd": 500.0,\n    })\n    response = await client.get("/api/v1/finops/budget/report/t-rpt")\n    assert response.status_code == 200\n    data = response.json()\n    assert data["tenant_id"] == "t-rpt"\n    assert "total_budget" in data\n